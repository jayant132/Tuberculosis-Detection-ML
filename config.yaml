data:
  root: data/images

image_size: 128
num_workers: 0
seed: 42

preprocess:
  use_clahe: true
  clahe_clip: 1.0
  clahe_tile: 8

split:
  val_ratio: 0.15
  test_ratio: 0.15

train:
  batch_size: 64
  lr: 0.001
  epochs: 50
  early_stop_patience: 8
